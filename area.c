#include<stdio.h>
float squarearea(float side);
float circleArea(float rad);
float rectArea(float a, float b);
int main()
{
   

   return 0;
}
float squarearea(float side)
{
     return side * side;
}
float circleArea(float rad)
{
    return 3.14* rad * rad;
}