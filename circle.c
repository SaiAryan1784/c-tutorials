#include<stdio.h>
int main() {
    int b;
    printf("enter the radius of circle");
    scanf("&d", &b);
//    printf("area of the circle is:%f", (float)(3.14*(float)b*(float)b));
    printf("area of the square is:%f", (float)(3.14*(float)b*(float)b));

    return 0;
}
/*include<stdio.h>
int main() {
    int a;
    printf("enter the length of the side:");
    scanf("%d", &a);
    printf("area of the square is:%f", (float)(3.14*(float)a*(float)a));
    return 0;*/
